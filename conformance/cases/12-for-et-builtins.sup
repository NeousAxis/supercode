mission m {
  for x in ["a", "b"] { log "{upper(x)}" }
  log "{sum([1,2,3])} {len("abc")} {join(sort([3,1,2]), "-")}"
  log "{join(unique([1,1,2]), ",")} {slice("abcdef", 1, 3)}"
  log "{to_json({a: 1})}"
}
